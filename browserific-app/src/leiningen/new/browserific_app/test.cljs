(ns {{name}}.core-test
    (:require-macros [cljs.test :as test :refer [test-var]
                      :refer-macros [are is deftest]]))


(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
